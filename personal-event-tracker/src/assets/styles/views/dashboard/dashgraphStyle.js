const dashgraphStyle = theme => ({
    paper: {
        marginTop: "6.5rem",
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    },
    button: {
        marginBottom: "1rem",
        width: "max-content"
    }
});

export default dashgraphStyle;