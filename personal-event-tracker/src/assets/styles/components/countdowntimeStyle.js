const countdowntimeStyle = theme => ({
    paper: {
        marginTop: "1rem",
        marginBottom: "1rem",
        [theme.breakpoints.up("sm")]: {
            alignSelf: "center"
        }
    },
});

export default countdowntimeStyle;

//[theme.breakpoints.up("sm")]: {