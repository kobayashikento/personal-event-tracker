const themepreviewStyle = theme => ({
    typo: {
        paddingLeft: theme.spacing(5),
        paddingTop: theme.spacing(3),
        width: "inherit",
    },
    grid: {
        paddingLeft: theme.spacing(5),
        paddingTop: theme.spacing(3),
    }
})

export default themepreviewStyle;